{
  "id": "data-table",
  "name": "Table: Warm Keynote",
  "description": "Chapter head and lede, then a glass container holding the table, its header in small monospace capitals, with a note below. A bulleted list would lose the alignment between columns.",
  "content_relations": [
    "comparison",
    "evidence",
    "list"
  ],
  "scene_roles": [
    "evidence"
  ],
  "density": {
    "max_chars": 400,
    "max_elements": 9
  },
  "slots": {
    "kicker": {
      "type": "text",
      "required": false,
      "hint": "chapter label, e.g. \"01 — Why\"; numbered automatically by the scaffold when the slides in the story carry a chapter, otherwise left empty (an empty label takes the vertical bar with it)"
    },
    "meta": {
      "type": "text",
      "required": false,
      "hint": "pill tag top right: audience, phase or occasion, up to 10 characters, filled automatically from the first clause of occasion; leave empty when not needed"
    },
    "title": {
      "type": "text",
      "required": true,
      "hint": "the claim of the slide in one sentence, up to 2 lines and 24 characters; *keyword* marks an emphasis"
    },
    "subtitle": {
      "type": "text",
      "required": false,
      "hint": "a one-line lede, up to 50 characters"
    },
    "table": {
      "type": "table",
      "required": true,
      "hint": "table: one header row, 3 to 6 rows of data, one line per cell"
    },
    "caption": {
      "type": "text",
      "required": false,
      "hint": "a one-line note"
    },
    "page": {
      "type": "text",
      "required": false,
      "hint": "page number pill, filled automatically (\"01 / 08\")"
    }
  },
  "elements": [
    {
      "id": "kicker",
      "kind": "text"
    },
    {
      "id": "meta",
      "kind": "text"
    },
    {
      "id": "title",
      "kind": "text"
    },
    {
      "id": "subtitle",
      "kind": "text"
    },
    {
      "id": "table",
      "kind": "text"
    },
    {
      "id": "caption",
      "kind": "text"
    },
    {
      "id": "progress",
      "kind": "shape"
    },
    {
      "id": "page",
      "kind": "text"
    }
  ],
  "sample": {
    "kicker": {
      "type": "text",
      "value": "01 — Why"
    },
    "meta": {
      "type": "text",
      "value": "Site owners"
    },
    "page": {
      "type": "text",
      "value": "03 / 08"
    },
    "title": {
      "type": "text",
      "value": "What the checker can do ends where its tool specs end"
    },
    "subtitle": {
      "type": "text",
      "value": "Every capability needs a tool spec of its own."
    },
    "table": {
      "type": "table",
      "header": [
        "Tool",
        "Purpose",
        "Input",
        "Output"
      ],
      "rows": [
        [
          "getUptime",
          "Read the uptime log",
          "siteId, date",
          "firstDown, lastUp"
        ],
        [
          "openIncident",
          "Open an incident",
          "siteId, date, type",
          "incidentId, status"
        ],
        [
          "getQuota",
          "Read the remaining quota",
          "siteId, checkType",
          "remainingRuns"
        ]
      ]
    },
    "caption": {
      "type": "text",
      "value": "Input and output are only the minimum."
    }
  }
}
