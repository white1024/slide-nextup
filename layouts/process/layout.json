{
  "id": "process",
  "name": "Process: four steps",
  "description": "A horizontal rail with four nodes and four step cards; a step card can hold a metric of number | step name | one-sentence note. A string of arrows would lose the readability of every step having its own name and note.",
  "content_relations": ["sequence"],
  "scene_roles": ["relationship", "map"],
  "density": { "max_chars": 400, "max_elements": 14 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically" },
    "title": { "type": "text", "required": true, "hint": "a one-line claim, up to 24 characters" },
    "step-1": { "type": ["text", "metric"], "required": true, "hint": "metric: value holds the number or step name, label holds a one-sentence note" },
    "step-2": { "type": ["text", "metric"], "required": true, "hint": "same as above" },
    "step-3": { "type": ["text", "metric"], "required": true, "hint": "same as above" },
    "step-4": { "type": ["text", "metric"], "required": false, "hint": "same as above; with only three steps leave it empty and hide the fourth node" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "title", "kind": "text" },
    { "id": "rail", "kind": "shape" },
    { "id": "node-1", "kind": "shape" },
    { "id": "node-2", "kind": "shape" },
    { "id": "node-3", "kind": "shape" },
    { "id": "node-4", "kind": "shape" },
    { "id": "step-1", "kind": "text" },
    { "id": "step-2", "kind": "text" },
    { "id": "step-3", "kind": "text" },
    { "id": "step-4", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "title": { "type": "text", "value": "Four steps, one stop for a person" },
    "step-1": { "type": "metric", "value": "01", "label": "Brief: purpose, length, material and density asked in one go" },
    "step-2": { "type": "metric", "value": "02", "label": "Story file: message and evidence per slide, *you confirm* before it moves on" },
    "step-3": { "type": "metric", "value": "03", "label": "Visual direction: three real covers, pick one" },
    "step-4": { "type": "metric", "value": "04", "label": "Generate and edit: HTML output, changed in the browser" },
    "page": { "type": "text", "value": "05 / 08" }
  }
}
