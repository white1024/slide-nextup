{
  "id": "section",
  "name": "Section",
  "description": "A large chapter number, a short colour bar, a title and one lede, the rest left blank. A regular title slide would lose the breathing space that says a new chapter starts here.",
  "content_relations": ["statement", "list"],
  "scene_roles": ["map", "pause", "relationship"],
  "density": { "max_chars": 160, "max_elements": 10 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically from the series part of the story title (before the colon)" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically from the first clause of occasion (up to 10 characters)" },
    "number": { "type": "text", "required": true, "hint": "chapter number, two digits, e.g. 02" },
    "title": { "type": "text", "required": true, "hint": "chapter title, 1 to 2 lines" },
    "body": { "type": "text", "required": false, "hint": "lede, 1 to 2 lines" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "corner-tl", "kind": "shape" },
    { "id": "corner-br", "kind": "shape" },
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "number", "kind": "text" },
    { "id": "rule", "kind": "shape" },
    { "id": "title", "kind": "text" },
    { "id": "body", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "number": { "type": "text", "value": "02" },
    "title": { "type": "text", "value": "The tool is fine; the order is wrong" },
    "body": { "type": "text", "value": "Today's tools make you lay out first and think about the story later, so every story change means a relayout." },
    "page": { "type": "text", "value": "03 / 08" }
  }
}
