{
  "id": "grid-2x2",
  "name": "Bento: 2x2 grid",
  "description": "Four equal cards for four parallel items or four numbers. A bullet list would lose the tidiness of four separate things taken in at a glance.",
  "content_relations": ["list", "evidence", "hierarchy"],
  "scene_roles": ["evidence", "map"],
  "density": { "max_chars": 400, "max_elements": 8 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically" },
    "title": { "type": "text", "required": true, "hint": "a one-line claim, up to 24 characters" },
    "card-1": { "type": ["text", "metric"], "required": true, "hint": "a short phrase or a big number" },
    "card-2": { "type": ["text", "metric"], "required": true, "hint": "same as above" },
    "card-3": { "type": ["text", "metric"], "required": true, "hint": "same as above" },
    "card-4": { "type": ["text", "metric"], "required": false, "hint": "same as above; leave empty when there is no fourth" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "title", "kind": "text" },
    { "id": "card-1", "kind": "text" },
    { "id": "card-2", "kind": "text" },
    { "id": "card-3", "kind": "text" },
    { "id": "card-4", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "title": { "type": "text", "value": "Four promises of the new flow" },
    "card-1": { "type": "text", "value": "Not one slide is generated before the story is confirmed." },
    "card-2": { "type": "text", "value": "Three visual directions are chosen from real covers, not adjectives." },
    "card-3": { "type": "metric", "value": "100%", "label": "of elements editable in the browser", "delta": "text, position, style, image" },
    "card-4": { "type": "text", "value": "Regenerating a slide always keeps manual edits." },
    "page": { "type": "text", "value": "06 / 08" }
  }
}
