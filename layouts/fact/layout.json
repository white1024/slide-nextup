{
  "id": "fact",
  "name": "Single number",
  "description": "One 300px number fills the slide, with a label, a delta and one sentence of explanation. A card would lose the weight of a slide with exactly one number to remember.",
  "content_relations": ["evidence", "statement"],
  "scene_roles": ["evidence", "hero", "pause"],
  "density": { "max_chars": 160, "max_elements": 10 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically" },
    "kicker": { "type": "text", "required": false, "hint": "the number's context, 4 to 12 characters" },
    "fact": { "type": ["metric", "text"], "required": true, "hint": "metric: value is the number, label says what it stands for, delta is the change or the base" },
    "caption": { "type": "text", "required": false, "hint": "one sentence of source or caveat" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "corner-tl", "kind": "shape" },
    { "id": "corner-br", "kind": "shape" },
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "kicker", "kind": "text" },
    { "id": "rule", "kind": "shape" },
    { "id": "fact", "kind": "text" },
    { "id": "caption", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "kicker": { "type": "text", "value": "Six decks, last quarter" },
    "fact": { "type": "metric", "value": "0", "label": "layout rounds across two trial decks on the new flow", "delta": "old flow averaged 4" },
    "caption": { "type": "text", "value": "Each trial revised its story once or twice; layout, zero." },
    "page": { "type": "text", "value": "06 / 08" }
  }
}
