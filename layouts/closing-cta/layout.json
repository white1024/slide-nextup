{
  "id": "closing-cta",
  "name": "Closing: call to action",
  "description": "A big title, one sentence and an underlined call to action, with a glow on the right that echoes the cover. A \"thank you\" slide would lose the single action the audience leaves with.",
  "content_relations": ["closing", "statement"],
  "scene_roles": ["close"],
  "density": { "max_chars": 180, "max_elements": 10 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "title": { "type": "text", "required": true, "hint": "closing claim, 1 to 2 lines, up to 20 characters" },
    "body": { "type": "text", "required": false, "hint": "1 to 2 lines of supplement" },
    "cta": { "type": "text", "required": false, "hint": "the one thing the audience does when they leave, in one sentence" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "glow", "kind": "shape" },
    { "id": "corner-tl", "kind": "shape" },
    { "id": "corner-br", "kind": "shape" },
    { "id": "brand", "kind": "text" },
    { "id": "rule", "kind": "shape" },
    { "id": "title", "kind": "text" },
    { "id": "body", "kind": "text" },
    { "id": "cta", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "title": { "type": "text", "value": "Next week, a *real* one" },
    "body": { "type": "text", "value": "Run a real proposal through this flow and we review the result together." },
    "cta": { "type": "text", "value": "Bring a topic; the flow does the rest." },
    "page": { "type": "text", "value": "08 / 08" }
  }
}
