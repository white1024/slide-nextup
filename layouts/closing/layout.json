{
  "id": "closing",
  "name": "Closing",
  "description": "Inverted background, one closing claim, a short explanation and one call to action. A regular content slide would lose the signal that the talk is over and a decision is due.",
  "content_relations": ["closing", "statement"],
  "scene_roles": ["close"],
  "density": { "max_chars": 160, "max_elements": 4 },
  "slots": {
    "title": { "type": "text", "required": true, "hint": "closing claim, one line" },
    "body": { "type": "text", "required": false, "hint": "1 to 2 lines of explanation" },
    "cta": { "type": "text", "required": true, "hint": "the one thing the audience should do" }
  },
  "elements": [
    { "id": "backdrop", "kind": "shape" },
    { "id": "title", "kind": "text" },
    { "id": "body", "kind": "text" },
    { "id": "cta", "kind": "text" }
  ],
  "sample": {
    "title": { "type": "text", "value": "Next week, a real one" },
    "body": { "type": "text", "value": "Run a real proposal through this flow and we review the result together." },
    "cta": { "type": "text", "value": "We need one volunteer with a topic" }
  }
}
