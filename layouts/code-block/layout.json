{
  "id": "code-block",
  "name": "Code block",
  "description": "A title, a block of monospaced code or commands (up to 10 lines of 40 characters) and a short note. Plain text would lose the character-by-character alignment.",
  "content_relations": ["evidence", "sequence", "statement"],
  "scene_roles": ["evidence", "relationship"],
  "density": { "max_chars": 800, "max_elements": 6 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically" },
    "title": { "type": "text", "required": true, "hint": "what this code proves, one line" },
    "code": { "type": "code", "required": true, "hint": "up to 10 lines, up to 40 characters per line; lang is only a label" },
    "caption": { "type": "text", "required": false, "hint": "one line of explanation" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "title", "kind": "text" },
    { "id": "code", "kind": "text" },
    { "id": "caption", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "title": { "type": "text", "value": "The tool holds the gate, not memory" },
    "code": {
      "type": "code",
      "lang": "bash",
      "value": "$ pnpm story:check decks/q3/story.md\npassed: 0 errors, 0 warnings\n\n$ pnpm deck:scaffold decks/q3/story.md\nrefused: the story is not confirmed (no story.confirmed.json)\n\n$ pnpm story:confirm decks/q3/story.md\nconfirmed: 8 slides, sha256 edce99f4"
    },
    "caption": { "type": "text", "value": "Unconfirmed, or edited since: the scaffold refuses outright; there is no way round it but --force." },
    "page": { "type": "text", "value": "05 / 08" }
  }
}
