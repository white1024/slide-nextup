{
  "id": "quote",
  "name": "Quote",
  "description": "One sentence fills the slide, with a colour bar on the left and the source below; for a pause or a question. A regular title slide would lose the weight of a slide that holds only this one sentence.",
  "content_relations": ["statement"],
  "scene_roles": ["pause", "hero", "close"],
  "density": { "max_chars": 140, "max_elements": 9 },
  "slots": {
    "brand": { "type": "text", "required": false, "hint": "top bar left, filled automatically" },
    "meta": { "type": "text", "required": false, "hint": "top bar right, filled automatically" },
    "title": { "type": "text", "required": true, "hint": "quote or question, 1 to 3 lines, up to 40 characters; *keyword* marks an emphasis" },
    "caption": { "type": "text", "required": false, "hint": "source or one added remark, one line" },
    "page": { "type": "text", "required": false, "hint": "page number, filled automatically" }
  },
  "elements": [
    { "id": "corner-tl", "kind": "shape" },
    { "id": "corner-br", "kind": "shape" },
    { "id": "brand", "kind": "text" },
    { "id": "meta", "kind": "text" },
    { "id": "bar", "kind": "shape" },
    { "id": "title", "kind": "text" },
    { "id": "caption", "kind": "text" },
    { "id": "page", "kind": "text" }
  ],
  "sample": {
    "brand": { "type": "text", "value": "Confirm the story before the slides" },
    "meta": { "type": "text", "value": "Weekly meeting pitch - 2026-09" },
    "title": { "type": "text", "value": "Last time you fixed a deck, was it the *content* or the layout?" },
    "caption": { "type": "text", "value": "Pause three to five seconds before moving on." },
    "page": { "type": "text", "value": "07 / 08" }
  }
}
